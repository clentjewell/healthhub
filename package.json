{
  "name": "healthhub-tweed-coast",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Health Hub Tweed Coast — static Astro site (Cloudflare Pages)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
